services:
  - type: web
    name: meridian
    runtime: docker
    plan: starter
    healthCheckPath: /
    envVars:
      - key: ONCHAINOS_BUNDLE
        sync: false
    disk:
      name: meridian-data
      mountPath: /data
      sizeGB: 1
