{
  "name": "meridian",
  "version": "0.1.0",
  "private": true,
  "description": "Pre-trade price and liquidity check for tokenized stocks on X Layer",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "sample": "tsx src/cli/sample-once.ts",
    "sampler": "tsx src/cli/sampler.ts",
    "probe": "tsx src/cli/probe.ts",
    "volume-study": "tsx src/cli/volume-study.ts"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.7.5",
    "tsx": "^4.19.1",
    "typescript": "^5.6.3"
  }
}
